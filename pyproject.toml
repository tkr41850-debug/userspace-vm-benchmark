[project]
name = "vmb"
version = "0.1.0"
description = "Rootless isolation technology benchmark suite for x86_64 Linux"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psutil>=7.2.2",
    "rich>=14.3.3",
]

[project.scripts]
vmb = "vmb.runner:main"
